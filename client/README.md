# GreenBi client app for customers

## Installation

### Required dependencies

Here is a list of the required dependencies to install with Node.js.

```
npm install -g @ionic/cli
npm install --save @stripe/react-stripe-js @stripe/stripe-js
```
